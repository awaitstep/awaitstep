[
  {
    "definition": {
      "id": "resend_send_email",
      "name": "Resend Send Email",
      "version": "1.0.0",
      "description": "Sends an email via the Resend API",
      "category": "Email",
      "tags": [
        "email",
        "resend",
        "transactional"
      ],
      "author": "awaitstep",
      "license": "Apache-2.0",
      "configSchema": {
        "from": {
          "type": "string",
          "label": "From",
          "description": "Sender email address. Must be a verified domain in Resend.",
          "required": true,
          "default": "onboarding@resend.dev",
          "placeholder": "you@example.com"
        },
        "to": {
          "type": "string",
          "label": "To",
          "description": "Recipient email address.",
          "required": true,
          "placeholder": "recipient@example.com"
        },
        "subject": {
          "type": "string",
          "label": "Subject",
          "required": true,
          "placeholder": "Welcome to AwaitStep"
        },
        "html": {
          "type": "textarea",
          "label": "HTML Body",
          "description": "Email body as HTML.",
          "required": true
        },
        "replyTo": {
          "type": "string",
          "label": "Reply To",
          "placeholder": "reply@example.com"
        },
        "cc": {
          "type": "string",
          "label": "CC",
          "placeholder": "cc@example.com"
        },
        "bcc": {
          "type": "string",
          "label": "BCC",
          "placeholder": "bcc@example.com"
        },
        "apiKey": {
          "type": "secret",
          "label": "Resend API Key",
          "description": "Found in your Resend dashboard under API Keys.",
          "required": true,
          "envVarName": "RESEND_API_KEY"
        }
      },
      "outputSchema": {
        "id": {
          "type": "string",
          "description": "Resend email ID"
        }
      },
      "providers": [
        "cloudflare"
      ],
      "runtime": {
        "defaultTimeout": "30 seconds",
        "defaultRetries": 3,
        "idempotent": false
      }
    },
    "templates": {
      "cloudflare": "export default async function (ctx) {\n  const body: Record<string, unknown> = {\n    from: ctx.config.from,\n    to: ctx.config.to,\n    subject: ctx.config.subject,\n    html: ctx.config.html,\n  }\n\n  if (ctx.config.replyTo) body.reply_to = ctx.config.replyTo\n  if (ctx.config.cc) body.cc = ctx.config.cc\n  if (ctx.config.bcc) body.bcc = ctx.config.bcc\n\n  const response = await fetch('https://api.resend.com/emails', {\n    method: 'POST',\n    headers: {\n      Authorization: `Bearer ${ctx.env.RESEND_API_KEY}`,\n      'Content-Type': 'application/json',\n    },\n    body: JSON.stringify(body),\n  })\n\n  if (!response.ok) {\n    const errorText = await response.text()\n    throw new Error(`Resend API error (${response.status}): ${errorText}`)\n  }\n\n  const data = (await response.json()) as { id: string }\n\n  return {\n    id: data.id,\n  }\n}\n"
    },
    "bundledAt": "2026-03-19T19:30:41.945Z",
    "checksum": "sha256:88c29a443d5615feea3c44e4dd50cc064c53420f7a7d290dad21edef206d9d5d"
  }
]
